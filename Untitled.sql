CREATE TABLE `follows` (
  `cpf` 12345678910 PRIMARY KEY,
  `nome` Matheus,
  `sobrenome` silva,
  `idade` 19
);

CREATE TABLE `cadastro_pj` (
  `cnpj` interger PRIMARY KEY,
  `nome` Matheus,
  `escricao` 362515,
  `ipoatividade` seila
);

CREATE TABLE `produto` (
  `cnpj` 123456789789 PRIMARY KEY,
  `nome` Mercado,
  `country_code` compras
);
